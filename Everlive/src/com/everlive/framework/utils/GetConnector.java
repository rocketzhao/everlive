package com.everlive.framework.utils;import java.sql.Connection;import java.sql.DriverManager;import java.sql.SQLException;import java.sql.Statement;import java.sql.Timestamp;import java.text.SimpleDateFormat;import java.util.Date;public class GetConnector {	/* 获取数据库连接的函数*/	public static Connection getConnection() {		Connection con = null;	//创建用于连接数据库的Connection对象		try {			Class.forName("com.mysql.jdbc.Driver");// 加载sqlserver数据驱动			con=DriverManager.getConnection("jdbc:mysql://192.168.7.175:3306/everlive2?user=root&password=zhaojun123");// 创建数据连接//			System.out.println("Connection Successful!"); //如果连接成功 控制台输出Connection Successful! 					} catch (Exception e) {			System.out.println("数据库连接失败" + e.getMessage());		}		return con;	//返回所建立的数据库连接	}		public static void main(String[] args){		String status = "suspend1";		Date date = new Date();//获得系统时间.        String nowTime = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss").format(date);//将时间格式转换成符合Timestamp要求的格式.        Timestamp logdate = Timestamp.valueOf(nowTime);//把时间转换		Integer userId = 1;		String sqlstart = "update el_user_star_status set logdate='"+logdate+"',status='"+status+"' where userId = "+userId;		Connection conn = GetConnector.getConnection();		try {			Statement st = (Statement) conn.createStatement();	//创建用于执行静态sql语句的Statement对象，st属局部变量//	        ResultSet rs = null;//			rs = st.executeQuery(sqlstart);//			ResultSetMetaData metaData = rs.getMetaData();//			int columnCount = metaData.getColumnCount();//			while (rs.next()) {//		        // 遍历每一列  //		        for (int i = 1; i <= columnCount; i++) {  //		            String columnName = metaData.getColumnLabel(i);//		            System.out.println(columnName);//		            System.out.println(rs.getObject(columnName));//		        }//		    }			int rs2 = st.executeUpdate(sqlstart);			System.out.println(rs2);		} catch (SQLException e) {			// TODO Auto-generated catch block			e.printStackTrace();		}			}}