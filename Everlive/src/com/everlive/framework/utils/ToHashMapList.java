package com.everlive.framework.utils;import java.sql.ResultSet;import java.sql.ResultSetMetaData;import java.sql.SQLException;import java.util.ArrayList;import java.util.HashMap;import java.util.List;public class ToHashMapList {	/* 返回HashMapList类型数据*/	public static List<HashMap<String,Object>> toHashMapList(ResultSet rs) {		// HashMap数组  	    List<HashMap<String,Object>> mapArray = new ArrayList<HashMap<String,Object>>();		// HashMap对象	    HashMap<String,Object> mapObj = new HashMap<String,Object>();  	    // 获取列数  		try {			ResultSetMetaData metaData = rs.getMetaData();			int columnCount = metaData.getColumnCount();  		    		    // 遍历ResultSet中的每条数据  		    while (rs.next()) {  		    	mapObj = new HashMap<String,Object>();  		        // 遍历每一列  		        for (int i = 1; i <= columnCount; i++) {  		            String columnName = metaData.getColumnLabel(i);  		            Object value = rs.getObject(columnName);  		            mapObj.put(columnName, value);  		        }		        mapArray.add(mapObj);   		    }  		} catch (SQLException e) {			// TODO Auto-generated catch block			e.printStackTrace();		}  	    return mapArray;	}}